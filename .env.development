# Defaults for `npm run dev`: demo fixtures on, so the UI works with no backend running.
# Toggle demo off in Settings (dev-only control) to exercise the real API.
VITE_API_URL=http://localhost:8000
VITE_DEMO=1
